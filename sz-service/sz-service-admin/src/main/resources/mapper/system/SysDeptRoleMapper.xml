<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sz.admin.system.mapper.SysDeptRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.sz.admin.system.pojo.po.SysDeptRole">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="dept_id" property="deptId"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_id, user_id
    </sql>

    <!-- 批量插入用户角色关联记录 -->
    <insert id="insertBatchSysDeptRole">
        INSERT INTO sys_dept_role (role_id, dept_id)
        VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{roleId}, #{deptId})
        </foreach>
    </insert>

</mapper>
