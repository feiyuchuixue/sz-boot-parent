<configuration>
    <!-- 关闭控制台输出logback配置信息的问题-->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />
    <!--服务名称-->
    <springProperty name="APP_NAME" source="spring.application.name"/>
    <!-- 定义日志文件的输出路径 -->
    <property name="LOG_PATH" value="./logs/${APP_NAME}"/>
    <!-- 文件保留最大天数 -->
    <property name="MAX_HISTORY" value="60"/>
    <!-- 文件大小限制 -->
    <property name="MAX_FILE_SIZE" value="500MB"/>
    <!-- 文件总大小 -->
    <property name="TOTAL_SIZE_CAP" value="5000MB"/>


    <!-- =========日志格式========= -->
    <!-- 彩色日志格式 -->
    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>
    <!-- 控制台输出格式（彩色）pattern-->
    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%clr(%t){cyan}] %clr(${PID:- }){magenta} [%clr(${APP_NAME:-}){blue}] [%X{traceId:-}] %highlight(%-5level) %magenta(%logger{30}:%-3L) - %m%n"/>
    <!-- 日志文件输出格式pattern-->
    <property name="CONSOLE_LOG_PATTERN_SIMPLE"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] ${PID:- } [${APP_NAME:-}] [%X{traceId:-}] %-5level %logger{30}:%-3L - %m%n"/>


    <!-- =========滚动日志输出========= -->
    <!-- 将日志滚动输出到app.log文件中 -->
    <appender name="APPLICATION"
              class="com.sz.logger.logbackadvice.SzRollingFileAppender">
        <!-- 输出文件目的地 -->
        <file>${LOG_PATH}/app.log</file>
        <encoder>
            <!-- 使用默认的输出格式打印 -->
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 过滤的级别 -->
            <level>INFO</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
        <!-- 设置 RollingPolicy 属性，用于配置文件大小限制，保留天数、文件名格式 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 文件命名格式 -->
            <fileNamePattern>${LOG_PATH}/app.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- 文件保留最大天数 -->
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <!-- 文件大小限制 -->
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <!-- 文件总大小 -->
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>
    <!-- 摘取出WARN级别日志输出到warn.log中 -->
    <appender name="WARN" class="com.sz.logger.logbackadvice.SzRollingFileAppender">
        <file>${LOG_PATH}/warn.log</file>
        <encoder>
            <!-- 使用默认的输出格式打印 -->
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <!-- 设置 RollingPolicy 属性，用于配置文件大小限制，保留天数、文件名格式 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 文件命名格式 -->
            <fileNamePattern>${LOG_PATH}/warn.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- 文件保留最大天数 -->
            <maxHistory>60</maxHistory>
            <!-- 文件大小限制 -->
            <maxFileSize>50MB</maxFileSize>
            <!-- 文件总大小 -->
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <!-- 日志过滤器，将WARN相关日志过滤出来 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 过滤的级别 -->
            <level>WARN</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>
    <!-- 摘取出ERROR级别日志输出到error.log中 -->
    <appender name="ERROR" class="com.sz.logger.logbackadvice.SzRollingFileAppender">
        <file>${LOG_PATH}/error.log</file>
        <encoder>
            <!-- 使用默认的输出格式打印 -->
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <!-- 设置 RollingPolicy 属性，用于配置文件大小限制，保留天数、文件名格式 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 文件命名格式 -->
            <fileNamePattern>${LOG_PATH}/error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- 文件保留最大天数 -->
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <!-- 文件大小限制 -->
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <!-- 文件总大小 -->
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
        <!-- 日志过滤器，将ERROR相关日志过滤出来 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>
    <!-- 配置控制台输出 -->
    <appender name="CONSOLE" class="com.sz.logger.logbackadvice.SzConsoleAppender">
        <encoder>
            <!-- 使用默认的输出格式打印 -->
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>
    <!-- HTTP访问审计日志。仅在有日志写入时创建文件，文件名按日期割接。 -->
    <appender name="AUDIT-ACCESS" class="com.sz.logger.logbackadvice.SzLazyRollingFileAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/audit/access.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- SQL审计日志。仅在有日志写入时创建文件，文件名按日期割接。 -->
    <appender name="AUDIT-SQL" class="com.sz.logger.logbackadvice.SzLazyRollingFileAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/audit/sql.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 操作审计主记录入库失败兜底日志。仅在有日志写入时创建文件，文件名按日期割接。 -->
    <appender name="AUDIT-OPERATION-FALLBACK" class="com.sz.logger.logbackadvice.SzLazyRollingFileAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/audit/operation-fallback.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>90</maxHistory>
            <maxFileSize>50MB</maxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 性能诊断兜底日志。仅在有日志写入时创建文件，文件名按日期割接。 -->
    <appender name="DIAGNOSTIC-PERFORMANCE" class="com.sz.logger.logbackadvice.SzLazyRollingFileAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/diagnostic/performance.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 异常诊断兜底日志。仅在有日志写入时创建文件，文件名按日期割接。 -->
    <appender name="DIAGNOSTIC-EXCEPTION" class="com.sz.logger.logbackadvice.SzLazyRollingFileAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN_SIMPLE}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/diagnostic/exception.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="audit-access-log" level="INFO" additivity="false">
        <appender-ref ref="AUDIT-ACCESS"/>
    </logger>
    <logger name="audit-sql-log" level="INFO" additivity="false">
        <appender-ref ref="AUDIT-SQL"/>
    </logger>
    <springProfile name="local,dev">
        <logger name="audit-sql-log" level="INFO" additivity="false">
            <appender-ref ref="AUDIT-SQL"/>
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>
    <logger name="audit-operation-fallback-log" level="INFO" additivity="false">
        <appender-ref ref="AUDIT-OPERATION-FALLBACK"/>
    </logger>
    <logger name="diagnostic-performance-log" level="INFO" additivity="false">
        <appender-ref ref="DIAGNOSTIC-PERFORMANCE"/>
    </logger>
    <logger name="diagnostic-exception-log" level="INFO" additivity="false">
        <appender-ref ref="DIAGNOSTIC-EXCEPTION"/>
    </logger>


    <!-- 配置输出级别 -->
    <root level="INFO">
        <!-- 加入控制台输出 -->
        <appender-ref ref="CONSOLE"/>
        <!-- 加入APPLICATION输出 -->
        <appender-ref ref="APPLICATION"/>
        <!-- 加入WARN日志输出 -->
        <appender-ref ref="WARN"/>
        <!-- 加入ERROR日志输出 -->
        <appender-ref ref="ERROR"/>
    </root>
</configuration>
